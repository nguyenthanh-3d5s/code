#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int a, b;
    string x;

    cin >> a >> b;
    // while(cin >> x)
    
    cin.ignore();
    getline(cin, x);

    cout << a << " " << b << " " << x << "\n";

    return 0;
}
