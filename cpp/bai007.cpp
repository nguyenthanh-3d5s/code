#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    float a = 0;
    float b = 3;

    printf("%.2f", -a/b ? -a/b : 0);

    return 0;
}
