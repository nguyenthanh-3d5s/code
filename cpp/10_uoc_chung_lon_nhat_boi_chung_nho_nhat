#include <bits/stdc++.h>
using namespace std;

using ll = long long;

int gcd(int, int);

int main() {
    #ifndef TEST
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    #endif

    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cout << gcd(10, 15) << endl;

    return 0;
}

int gcd(int a, int b) {
    while(b) {
        int tmp = a % b;
        a = b;
        b = tmp;
    }

    return a;
}
