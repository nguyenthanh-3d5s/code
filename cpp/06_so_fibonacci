#include <bits/stdc++.h>
using namespace std;

using ll = long long;
const int N = 90;

void fibonacci(ll []);

int main() {
    #ifndef TEST
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    #endif

    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    ll fibo[N];
    fibonacci(fibo);

    for (int i = 0; i < N; i++) {
        cout << fibo[i] << " ";
    }
    cout << endl;

    return 0;
}

void fibonacci(ll fibo[]) {
    fibo[0] = 0;
    fibo[1] = 1;
    for (int i = 2; i < N; i++) {
        fibo[i] = fibo[i - 1] + fibo[i - 2];
    }
}
