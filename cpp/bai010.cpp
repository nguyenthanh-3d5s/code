#include <bits/stdc++.h>
using namespace std;

#define sz(a) (int) (a).size()

typedef long long ll;
typedef vector<int> vi;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    string s = "monkey";
    
    sort(s.begin(),s.end());

    cout << s << "\n";

    return 0;
}
